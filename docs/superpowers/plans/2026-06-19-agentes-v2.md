# Aba Agentes v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reformular a aba Agentes: remover status ao vivo, substituir tabela de 7 colunas + linha expansível por tabela com badges coloridos + pontuação + modal "Perfil do Agente" com gráfico e tabela mensal; tornar o fetch 100% paralelo com lazy loading de dados mensais.

**Architecture:** Todas as mudanças em `index.html` (single-file). Remoção de funções e HTML da v1; substituição por novas funções. Fetch inicial paralelo (summaries + CSAT simultâneos); dados mensais carregados sob demanda ao abrir o modal, com cache por agente/período.

**Tech Stack:** HTML/CSS/JS inline, Chart.js 4 (já carregado via CDN com plugin de annotation), `api()`, `fmtSec()`, `esc()`, `chartColors()`, `destroyChart()` — todos já existem.

## Global Constraints

- Single file: todas as mudanças em `c:\Users\Samuel Wallace\Documents\vscode\grv-chatwoot-dashboard\index.html`
- CSS vars: `--bg`, `--bg2`, `--bg3`, `--border`, `--accent`, `--text`, `--text2`, `--text3`, `--green`, `--yellow`, `--red`
- SLAs: `_SLA_TMA` (horas), `_SLA_TMR` (minutos), `_SLA_FCR` (%), `_SLA_MSG` (msgs/conv)
- Linguagem sem jargão nas colunas: "Atendimento" (não TMA), "1ª Resposta" (não TMR), "Resolução" (não FCR)
- Pontuação 0–100: cada uma das 4 métricas vale 25pts (dentro SLA), 12pts (até 50% acima), 0pts (mais de 50%)
- Cache de meses: `_agentMonthCache[agentId][periodKey]` — não re-fetcha se já existe

---

## Task 1: CSS — Remover v1, Adicionar v2 (badges + modal)

**Files:**
- Modify: `index.html` (bloco `<style>`)

**Interfaces:**
- Produces: classes `.agent-badge`, `.badge-green`, `.badge-yellow`, `.badge-red`, `.badge-na`, `.score-badge`, `.score-hi`, `.score-mid`, `.score-lo`, `.ver-perfil-btn`, `.am-*` para o modal

- [ ] **Step 1: Substituir bloco CSS da aba Agentes**

Localizar o bloco que começa em `/* AGENTES TAB */` (linha ~172). Ele vai até `.csat-placeholder strong { ... }`. Substituir TODO esse bloco por:

```css
/* AGENTES TAB */
.agent-period-pills { display:flex; gap:4px; }
.agent-pill { padding:4px 14px; border-radius:20px; border:1px solid var(--border); background:transparent; color:var(--text2); font-size:12px; cursor:pointer; transition:background .15s,color .15s,border-color .15s; }
.agent-pill:hover { border-color:var(--accent); color:var(--accent); }
.agent-pill.active { background:var(--accent); color:#fff; border-color:var(--accent); }
.agent-table { width:100%; border-collapse:collapse; font-size:13px; }
.agent-table th { background:var(--bg2); color:var(--text2); font-size:11px; font-weight:600; text-align:left; padding:8px 10px; border-bottom:2px solid var(--border); cursor:pointer; user-select:none; white-space:nowrap; }
.agent-table th:hover { color:var(--text); }
.agent-table th.sort-asc::after  { content:' ▲'; color:var(--accent); }
.agent-table th.sort-desc::after { content:' ▼'; color:var(--accent); }
.agent-table td { padding:8px 10px; border-bottom:1px solid var(--border); vertical-align:middle; }
.agent-table tr:last-child td { border:none; }
.agent-table tr.agent-row:hover { background:var(--bg3); cursor:pointer; }
.agent-no-data { color:var(--text3); font-size:12px; font-style:italic; padding:24px; text-align:center; }
.agent-badge { display:inline-flex; align-items:center; gap:4px; padding:3px 8px; border-radius:12px; font-size:12px; font-weight:600; white-space:nowrap; }
.badge-green  { background:rgba(34,197,94,.15);  color:#22c55e; }
.badge-yellow { background:rgba(234,179,8,.15);  color:#eab308; }
.badge-red    { background:rgba(239,68,68,.15);  color:#ef4444; }
.badge-na     { background:var(--bg3); color:var(--text3); }
.score-badge  { display:inline-block; padding:2px 10px; border-radius:10px; font-size:13px; font-weight:800; }
.score-hi  { background:rgba(34,197,94,.15);  color:#22c55e; }
.score-mid { background:rgba(234,179,8,.15);  color:#eab308; }
.score-lo  { background:rgba(239,68,68,.15);  color:#ef4444; }
.ver-perfil-btn { padding:4px 12px; border-radius:5px; border:1px solid var(--border); background:transparent; color:var(--text2); font-size:12px; cursor:pointer; white-space:nowrap; }
.ver-perfil-btn:hover { border-color:var(--accent); color:var(--accent); }
#agp { position:fixed; z-index:300; background:var(--bg2); border:1px solid var(--border); border-radius:8px; box-shadow:0 8px 24px rgba(0,0,0,.4); width:260px; display:none; flex-direction:column; max-height:360px; }
#agp.open { display:flex; }
.agp-head { display:flex; align-items:center; gap:6px; padding:10px 12px; border-bottom:1px solid var(--border); }
.agp-head-title { flex:1; font-size:13px; font-weight:600; }
.agp-xs { padding:2px 8px; border-radius:4px; border:1px solid var(--border); background:transparent; color:var(--text2); font-size:11px; cursor:pointer; }
.agp-xs:hover { border-color:var(--accent); color:var(--accent); }
.agp-body { overflow-y:auto; flex:1; padding:6px 0; }
.agp-item { display:flex; align-items:center; gap:8px; padding:7px 12px; cursor:pointer; font-size:13px; }
.agp-item:hover { background:var(--bg3); }
.agp-item input[type=checkbox] { accent-color:var(--accent); width:14px; height:14px; flex-shrink:0; cursor:pointer; }
.agp-foot { display:flex; justify-content:flex-end; gap:8px; padding:10px 12px; border-top:1px solid var(--border); }
.agp-ok { padding:5px 16px; background:var(--accent); border:none; border-radius:5px; color:#fff; font-size:12px; font-weight:600; cursor:pointer; }
.agp-ok:hover { opacity:.85; }
.csat-placeholder { background:var(--bg2); border:1px solid var(--border); border-radius:8px; padding:24px; text-align:center; color:var(--text2); font-size:13px; line-height:1.7; }
.csat-placeholder strong { color:var(--text); display:block; margin-bottom:6px; }
/* MODAL AGENTE */
#agent-modal-backdrop { display:none; position:fixed; inset:0; background:rgba(0,0,0,.6); z-index:400; align-items:center; justify-content:center; }
#agent-modal-backdrop.open { display:flex; }
#agent-modal { background:var(--bg2); border:1px solid var(--border); border-radius:12px; width:min(860px,95vw); max-height:90vh; overflow-y:auto; box-shadow:0 24px 64px rgba(0,0,0,.5); display:flex; flex-direction:column; }
.am-header { display:flex; align-items:center; gap:12px; padding:20px 24px 16px; border-bottom:1px solid var(--border); flex-wrap:wrap; }
.am-avatar { width:40px; height:40px; border-radius:50%; background:var(--accent); color:#fff; font-size:16px; font-weight:800; display:flex; align-items:center; justify-content:center; flex-shrink:0; }
.am-name { font-size:16px; font-weight:700; }
.am-semaphores { display:flex; gap:6px; align-items:center; flex-wrap:wrap; }
.am-nav { display:flex; gap:4px; margin-left:auto; }
.am-nav button { width:32px; height:32px; border-radius:6px; border:1px solid var(--border); background:transparent; color:var(--text); cursor:pointer; font-size:14px; }
.am-nav button:hover:not(:disabled) { border-color:var(--accent); color:var(--accent); }
.am-nav button:disabled { opacity:.3; cursor:default; }
.am-close { width:32px; height:32px; border-radius:6px; border:1px solid var(--border); background:transparent; color:var(--text2); cursor:pointer; font-size:18px; line-height:1; }
.am-close:hover { border-color:var(--red); color:var(--red); }
.am-body { padding:20px 24px 24px; display:flex; flex-direction:column; gap:20px; }
.am-highlights { display:flex; gap:10px; flex-wrap:wrap; }
.am-highlight { background:var(--bg3); border-radius:8px; padding:10px 14px; font-size:12px; flex:1; min-width:160px; line-height:1.5; }
.am-highlight strong { display:block; font-size:13px; margin-bottom:2px; }
.am-chart-wrap { height:240px; position:relative; }
.am-month-table { width:100%; border-collapse:collapse; font-size:12px; }
.am-month-table th { background:var(--bg3); color:var(--text2); font-size:11px; font-weight:600; padding:7px 10px; text-align:left; border-bottom:2px solid var(--border); }
.am-month-table td { padding:7px 10px; border-bottom:1px solid var(--border); }
.am-month-table tr:last-child td { border:none; }
```

- [ ] **Step 2: Verificar no browser**

Abrir http://localhost:8765. A aba Agentes ainda funciona (mesmo que visual v1 — as classes novas ainda não estão em uso, mas as antigas foram removidas — verificar que não há erro CSS no console).

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "style(agentes): substituir CSS v1 por CSS v2 — badges, score, modal"
```

---

## Task 2: HTML — Simplificar `#agentes-page` + Adicionar Modal

**Files:**
- Modify: `index.html` (HTML body)

**Interfaces:**
- Consumes: CSS do Task 1
- Produces: `#agentes-page` simplificado, `#agent-modal-backdrop`, `#agent-modal`, `#am-header`, `#am-highlights`, `#am-chart`, `#am-month-table-wrap`

- [ ] **Step 1: Substituir conteúdo interno do `#agentes-page`**

Localizar este bloco (linhas 764–774):

```html
  <div class="section-divider"><span class="section-divider-label">Status ao vivo</span></div>
  <div id="agent-live-section"><div class="agent-no-data">Carregando…</div></div>

  <div class="section-divider" style="margin-top:16px"><span class="section-divider-label">Desempenho no período</span></div>
  <div id="agent-perf-section"><div class="agent-no-data">Carregando…</div></div>

  <div class="section-divider" style="margin-top:16px"><span class="section-divider-label">Satisfação do Cliente (CSAT)</span></div>
  <div id="agent-csat-section"><div class="agent-no-data">Carregando…</div></div>
</div>
```

Substituir por:

```html
  <div id="agent-perf-section"><div class="agent-no-data">Carregando…</div></div>
  <div id="agent-csat-section" style="margin-top:16px"></div>
</div>
```

- [ ] **Step 2: Adicionar modal antes de `<!-- AGENT FILTER POPUP -->`**

Localizar `<!-- AGENT FILTER POPUP -->` e adicionar este bloco ANTES:

```html
<!-- AGENT MODAL -->
<div id="agent-modal-backdrop" onclick="closeAgentModal(event)">
  <div id="agent-modal" role="dialog" aria-modal="true" aria-label="Perfil do agente">
    <div class="am-header" id="am-header"></div>
    <div class="am-body">
      <div class="am-highlights" id="am-highlights"></div>
      <div class="am-chart-wrap"><canvas id="am-chart"></canvas></div>
      <div id="am-month-loading" class="agent-no-data" style="display:none">Carregando histórico…</div>
      <div id="am-month-table-wrap"></div>
    </div>
  </div>
</div>

```

- [ ] **Step 3: Verificar no browser**

Abrir http://localhost:8765, clicar em "Agentes". A seção "Status ao vivo" deve ter desaparecido. Console sem erros.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(agentes): simplificar HTML — remover status ao vivo, adicionar modal"
```

---

## Task 3: Estado + Funções Utilitárias (calcAgentScore, agentBadge)

**Files:**
- Modify: `index.html` (bloco JS — variáveis de estado e funções utilitárias)

**Interfaces:**
- Produces:
  - `_agentMonthCache: {}` — `{agentId: {periodKey: months[]}}`
  - `_agentModalId: null` — id do agente com modal aberto
  - `_agentModalOrder: []` — ids na ordem atual da tabela
  - `calcAgentScore(summary) → number|null` — 0–100 ou null se sem dados
  - `agentBadge(value, label, meta, unit, lowerIsBetter) → string` — HTML do badge

- [ ] **Step 1: Atualizar bloco de variáveis de estado da aba Agentes**

Localizar (linha ~870):
```javascript
/* ── AGENTES TAB STATE ── */
let _agentTabPeriod  = '3';    /* '1'|'3'|'6'|'12' */
let _agentTabFilter  = null;   /* Set<number>|null — ids de agentes */
let _agentList       = [];     /* [{id, name, availability_status, ...}] */
let _agentPerfData   = null;   /* {id: {summary, months[]}} */
let _agentOverview   = null;   /* dados brutos do overview */
let _agentCsat       = null;   /* [{agent_id, agent_name, total, positive}] | null */
let _agentExpanded   = null;   /* id do agente expandido */
let _agentTabDirty   = false;  /* true → re-fetch ao entrar na aba */
let _agentPerfSort   = { col: 'conversations_count', dir: 'desc' };
let _agp             = { sel: new Set() }; /* estado interno do popup */
```

Substituir por:

```javascript
/* ── AGENTES TAB STATE ── */
let _agentTabPeriod  = '3';    /* '1'|'3'|'6'|'12' */
let _agentTabFilter  = null;   /* Set<number>|null — ids de agentes */
let _agentList       = [];     /* [{id, name, ...}] */
let _agentPerfData   = null;   /* {agentId: {agent, summary}} */
let _agentCsat       = null;   /* [{id, name, total, positive}] | null */
let _agentTabDirty   = false;  /* true → re-fetch ao entrar na aba */
let _agentPerfSort   = { col: 'score', dir: 'desc' };
let _agentMonthCache = {};     /* {agentId: {periodKey: months[]}} — lazy */
let _agentModalId    = null;   /* id do agente com modal aberto */
let _agentModalOrder = [];     /* ids na ordem da tabela — para navegar ‹ › */
let _agp             = { sel: new Set() }; /* estado interno do popup */
```

- [ ] **Step 2: Adicionar calcAgentScore() e agentBadge()**

Localizar `function isAuthError(e)` (próximo de linha 1993) e adicionar ANTES dela:

```javascript
function calcAgentScore(summary) {
  if (!summary || (summary.conversations_count || 0) === 0) return null;
  const s = summary;
  let pts = 0;

  const tmaH = s.avg_resolution_time != null ? s.avg_resolution_time / 3600 : null;
  if (tmaH != null) {
    if (tmaH <= _SLA_TMA)             pts += 25;
    else if (tmaH <= _SLA_TMA * 1.5)  pts += 12;
  }

  const tmrM = s.avg_first_response_time != null ? s.avg_first_response_time / 60 : null;
  if (tmrM != null) {
    if (tmrM <= _SLA_TMR)             pts += 25;
    else if (tmrM <= _SLA_TMR * 1.5)  pts += 12;
  }

  const fcr = s.conversations_count > 0 ? (s.resolutions_count || 0) / s.conversations_count * 100 : null;
  if (fcr != null) {
    if (fcr >= _SLA_FCR)              pts += 25;
    else if (fcr >= _SLA_FCR - 15)    pts += 12;
  }

  const totalMsgs = (s.incoming_messages_count || 0) + (s.outgoing_messages_count || 0);
  const msgsConv  = s.conversations_count > 0 ? totalMsgs / s.conversations_count : null;
  if (msgsConv != null) {
    if (msgsConv <= _SLA_MSG)          pts += 25;
    else if (msgsConv <= _SLA_MSG * 1.5) pts += 12;
  }

  return pts;
}

function agentBadge(value, label, meta, unit, lowerIsBetter) {
  if (value == null || label == null) return '<span class="agent-badge badge-na">—</span>';
  const ratio = lowerIsBetter ? value / meta : meta / value;
  const cls   = ratio <= 1 ? 'badge-green' : ratio <= 1.5 ? 'badge-yellow' : 'badge-red';
  const icon  = ratio <= 1 ? '✓' : ratio <= 1.5 ? '~' : '✗';
  return `<span class="agent-badge ${cls}" title="meta: ${meta}${unit}">${label} ${icon}</span>`;
}
```

- [ ] **Step 3: Verificar no console do browser**

Abrir http://localhost:8765, abrir console e digitar:
```javascript
calcAgentScore({ conversations_count: 10, resolutions_count: 9, avg_resolution_time: 3600*4, avg_first_response_time: 60*20, incoming_messages_count: 50, outgoing_messages_count: 50 })
// Esperado: 100 (todas as métricas dentro da SLA)
calcAgentScore({ conversations_count: 10, resolutions_count: 5, avg_resolution_time: 3600*12, avg_first_response_time: 60*50, incoming_messages_count: 120, outgoing_messages_count: 80 })
// Esperado: 0 (todas as métricas fora da SLA por mais de 50%)
calcAgentScore(null)
// Esperado: null
```

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(agentes): calcAgentScore(), agentBadge() e atualizar variáveis de estado"
```

---

## Task 4: fetchAgentTab() — Fetch 100% Paralelo

**Files:**
- Modify: `index.html` (função `fetchAgentTab()`)

**Interfaces:**
- Consumes: `api()`, `getAgentTabRange()`, `_agentList`, `_agentTabFilter`, `_agentTabPeriod`, `cfg.account`
- Produces: `_agentPerfData` populado com `{agentId: {agent, summary}}` — SEM `months` (lazy), `_agentCsat` processado

- [ ] **Step 1: Substituir fetchAgentTab() completa**

Localizar `async function fetchAgentTab()` (linha ~2000) e substituir a função inteira (até o `}` de fechamento na linha ~2076) por:

```javascript
async function fetchAgentTab() {
  const perfEl = document.getElementById('agent-perf-section');
  const csatEl = document.getElementById('agent-csat-section');
  if (perfEl) perfEl.innerHTML = '<div class="agent-no-data">Carregando…</div>';
  if (csatEl) csatEl.innerHTML = '';

  if (!_agentList.length) {
    const agents = await api(`/v1/accounts/${cfg.account}/agents`).catch(() => []);
    if (Array.isArray(agents)) _agentList = agents;
  }

  const visibleAgents = (_agentTabFilter && _agentTabFilter.size > 0)
    ? _agentList.filter(a => _agentTabFilter.has(a.id))
    : _agentList;

  const { since, until } = getAgentTabRange();

  const [summaries, csatRaw] = await Promise.all([
    Promise.all(visibleAgents.map(a =>
      api(`/v2/accounts/${cfg.account}/reports/summary?type=agent&id=${a.id}&since=${since}&until=${until}`)
        .catch(() => null)
    )),
    api(`/v1/accounts/${cfg.account}/reports/agents/satisfaction?since=${since}&until=${until}`)
      .catch(() => null)
  ]);

  if (summaries[0] && summaries[0].error && isAuthError({ status: summaries[0].error })) {
    _agentPerfData = { _authError: true };
  } else {
    _agentPerfData = {};
    visibleAgents.forEach((a, i) => {
      _agentPerfData[a.id] = { agent: a, summary: summaries[i] };
    });
  }

  if (Array.isArray(csatRaw) && csatRaw.length) {
    const byAgent = {};
    csatRaw.forEach(r => {
      const id = r.assigned_agent?.id;
      if (!id) return;
      if (!byAgent[id]) byAgent[id] = { id, name: r.assigned_agent?.name || '—', total: 0, positive: 0 };
      byAgent[id].total++;
      if (r.rating === 'Satisfied') byAgent[id].positive++;
    });
    _agentCsat = Object.values(byAgent);
  } else {
    _agentCsat = null;
  }

  const updEl = document.getElementById('agent-tab-updated');
  if (updEl) updEl.textContent = `Atualizado às ${new Date().toLocaleTimeString('pt-BR')}`;

  renderAgentTab();
}
```

- [ ] **Step 2: Verificar performance no browser**

Abrir http://localhost:8765, abrir DevTools → Network, clicar em "Agentes". Verificar que as chamadas de summary aparecem em paralelo (timestamps similares), e que NÃO há chamada para `/reports/overview`. O carregamento deve ser visivelmente mais rápido.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "perf(agentes): fetch 100% paralelo — summary + CSAT simultâneos, sem overview"
```

---

## Task 5: renderAgentPerf() — Tabela com Badges, Pontuação e Botão "Ver perfil"

**Files:**
- Modify: `index.html` (função `renderAgentPerf()`)

**Interfaces:**
- Consumes: `_agentPerfData`, `_agentPerfSort`, `calcAgentScore()`, `agentBadge()`, `fmtSec()`, `esc()`, `_SLA_TMA`, `_SLA_TMR`, `_SLA_FCR`
- Produces: tabela renderizada, `_agentModalOrder` populado, `openAgentModal(id)` chamado ao clicar

- [ ] **Step 1: Substituir renderAgentPerf() completa**

Localizar `function renderAgentPerf()` e substituir toda a função (até seu `}` de fechamento, que inclui `if (_agentExpanded !== null) renderAgentExpandChart(_agentExpanded);`) por:

```javascript
function renderAgentPerf() {
  const el = document.getElementById('agent-perf-section');
  if (!el) return;

  if (!_agentPerfData) { el.innerHTML = '<div class="agent-no-data">Carregando…</div>'; return; }
  if (_agentPerfData._authError) {
    el.innerHTML = '<div class="agent-no-data">🔒 Dados por agente requerem permissão de Administrador no Chatwoot</div>';
    return;
  }

  const entries = Object.values(_agentPerfData).filter(e => e.agent);
  if (!entries.length) { el.innerHTML = '<div class="agent-no-data">Nenhum agente encontrado</div>'; return; }

  const { col, dir } = _agentPerfSort;
  const getVal = e => {
    const s = e.summary;
    if (!s) return null;
    if (col === 'conversations_count')     return s.conversations_count || 0;
    if (col === 'avg_resolution_time')     return s.avg_resolution_time ?? Infinity;
    if (col === 'avg_first_response_time') return s.avg_first_response_time ?? Infinity;
    if (col === 'fcr')                     return s.conversations_count > 0 ? (s.resolutions_count || 0) / s.conversations_count : null;
    if (col === 'score')                   return calcAgentScore(s);
    return null;
  };

  const sorted = [...entries].sort((a, b) => {
    const nd = e => !e.summary || (e.summary.conversations_count || 0) === 0;
    if (nd(a) && nd(b)) return 0;
    if (nd(a)) return 1;
    if (nd(b)) return -1;
    const va = getVal(a) ?? (dir === 'asc' ? Infinity : -Infinity);
    const vb = getVal(b) ?? (dir === 'asc' ? Infinity : -Infinity);
    return dir === 'asc' ? va - vb : vb - va;
  });

  _agentModalOrder = sorted.map(e => e.agent.id);

  const scoreCls = sc => sc == null ? 'badge-na' : sc >= 75 ? 'score-hi' : sc >= 50 ? 'score-mid' : 'score-lo';
  const th = (label, colId, tip) => {
    const cls = _agentPerfSort.col === colId ? (_agentPerfSort.dir === 'asc' ? 'sort-asc' : 'sort-desc') : '';
    return `<th class="${cls}" onclick="sortAgentPerf('${colId}')" title="${tip || ''}">${label}</th>`;
  };

  el.innerHTML = `
    <div class="chart-card" style="overflow-x:auto">
      <table class="agent-table">
        <thead><tr>
          ${th('Agente',      'name',                   'Nome do agente')}
          ${th('Volume',      'conversations_count',    'Total de conversas no período')}
          ${th('Atendimento', 'avg_resolution_time',    'Tempo Médio de Atendimento (TMA) · meta: ' + _SLA_TMA + 'h')}
          ${th('1ª Resposta', 'avg_first_response_time','Tempo Médio de 1ª Resposta (TMR) · meta: ' + _SLA_TMR + 'min')}
          ${th('Resolução',   'fcr',                    'Taxa de Resolução · meta: ' + _SLA_FCR + '%')}
          ${th('Pontuação',   'score',                  'Pontuação geral 0–100 baseada nas SLAs')}
          <th></th>
        </tr></thead>
        <tbody>
          ${sorted.map(e => {
            const s    = e.summary;
            const nd   = !s || (s.conversations_count || 0) === 0;
            const sc   = nd ? null : calcAgentScore(s);
            const tmaH = s?.avg_resolution_time != null ? s.avg_resolution_time / 3600 : null;
            const tmrM = s?.avg_first_response_time != null ? s.avg_first_response_time / 60 : null;
            const fcr  = !nd && s.conversations_count > 0
              ? (s.resolutions_count || 0) / s.conversations_count * 100 : null;
            /* Badge de Resolução: inverte lógica — maior é melhor.
               Passa (100-fcr) vs (100-SLA_FCR) com lowerIsBetter=true para reutilizar agentBadge */
            const fcrBadge = fcr == null
              ? '<span class="agent-badge badge-na">—</span>'
              : agentBadge(100 - fcr, Math.round(fcr) + '%', 100 - _SLA_FCR, '%', true);
            return `<tr class="agent-row" onclick="openAgentModal(${e.agent.id})">
              <td style="font-weight:600">${esc(e.agent.name)}</td>
              <td>${nd ? '—' : (s.conversations_count).toLocaleString('pt-BR')}</td>
              <td>${nd ? '<span class="agent-badge badge-na">—</span>' : agentBadge(tmaH, fmtSec(s.avg_resolution_time), _SLA_TMA, 'h', true)}</td>
              <td>${nd ? '<span class="agent-badge badge-na">—</span>' : agentBadge(tmrM, fmtSec(s.avg_first_response_time), _SLA_TMR, 'min', true)}</td>
              <td>${fcrBadge}</td>
              <td>${sc == null ? '<span class="agent-badge badge-na">—</span>' : `<span class="score-badge ${scoreCls(sc)}">${sc}</span>`}</td>
              <td><button class="ver-perfil-btn" onclick="event.stopPropagation();openAgentModal(${e.agent.id})">Ver perfil →</button></td>
            </tr>`;
          }).join('')}
        </tbody>
      </table>
    </div>`;
}
```

- [ ] **Step 2: Atualizar sortAgentPerf() para incluir 'score'**

Localizar `function sortAgentPerf(col)` e substituir:

```javascript
function sortAgentPerf(col) {
  if (_agentPerfSort.col === col) {
    _agentPerfSort.dir = _agentPerfSort.dir === 'asc' ? 'desc' : 'asc';
  } else {
    _agentPerfSort = {
      col,
      dir: (col === 'avg_resolution_time' || col === 'avg_first_response_time') ? 'asc' : 'desc'
    };
  }
  renderAgentPerf();
}
```

- [ ] **Step 3: Verificar no browser**

Abrir http://localhost:8765, clicar em "Agentes". A tabela deve mostrar:
- Badges coloridos em Atendimento, 1ª Resposta e Resolução
- Coluna Pontuação com número colorido (verde/amarelo/vermelho)
- Botão "Ver perfil →" em cada linha
- Ordenação padrão: Pontuação decrescente (melhor agente no topo)
- Clicar em cabeçalho de coluna reordena (console vai mostrar erro `openAgentModal is not defined` — isso é esperado até Task 6)

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat(agentes): tabela v2 — badges coloridos, pontuação 0-100, botão Ver perfil"
```

---

## Task 6: Modal "Perfil do Agente" — Todas as Funções

**Files:**
- Modify: `index.html` (JS — novas funções do modal)

**Interfaces:**
- Consumes: `_agentPerfData`, `_agentMonthCache`, `_agentModalId`, `_agentModalOrder`, `_agentTabPeriod`, `calcAgentScore()`, `agentBadge()`, `fmtSec()`, `esc()`, `chartColors()`, `destroyChart()`, `getAgentMonths()`, `api()`, `cfg.account`, `_SLA_TMA`, `_SLA_TMR`, `_SLA_FCR`
- Produces: `openAgentModal(id)`, `closeAgentModal(e)`, `navigateAgentModal(dir)`, `fetchAgentMonths(id)`, `renderAgentModalHeader(id)`, `renderAgentModalHighlights(id, months)`, `renderAgentModalChart(id)`, `renderAgentModalMonthTable(id)`

- [ ] **Step 1: Adicionar funções de controle do modal**

Localizar `function renderAgentTab()` e adicionar ANTES dela:

```javascript
async function openAgentModal(id) {
  _agentModalId = id;
  const backdrop = document.getElementById('agent-modal-backdrop');
  if (!backdrop) return;
  backdrop.classList.add('open');
  document.addEventListener('keydown', agentModalKeydown);
  renderAgentModalHeader(id);
  renderAgentModalHighlights(id, null);
  document.getElementById('am-month-table-wrap').innerHTML = '';
  destroyChart('am');
  await fetchAgentMonths(id);
  renderAgentModalHighlights(id, _agentMonthCache[id]?.[_agentTabPeriod]);
  renderAgentModalChart(id);
  renderAgentModalMonthTable(id);
}

function closeAgentModal(e) {
  if (e && e.target !== document.getElementById('agent-modal-backdrop')) return;
  _agentModalId = null;
  document.getElementById('agent-modal-backdrop')?.classList.remove('open');
  document.removeEventListener('keydown', agentModalKeydown);
  destroyChart('am');
}

function agentModalKeydown(e) {
  if (e.key === 'Escape')      { closeAgentModal(null); return; }
  if (e.key === 'ArrowLeft')   navigateAgentModal(-1);
  if (e.key === 'ArrowRight')  navigateAgentModal(1);
}

function navigateAgentModal(dir) {
  const idx  = _agentModalOrder.indexOf(_agentModalId);
  if (idx === -1) return;
  const next = _agentModalOrder[idx + dir];
  if (next != null) openAgentModal(next);
}
```

- [ ] **Step 2: Adicionar fetchAgentMonths() — lazy + cache**

Adicionar logo após as funções do Step 1:

```javascript
async function fetchAgentMonths(id) {
  const key = _agentTabPeriod;
  if (_agentMonthCache[id]?.[key]) return;
  const months  = getAgentMonths();
  const loadEl  = document.getElementById('am-month-loading');
  if (loadEl) loadEl.style.display = '';
  const results = await Promise.all(months.map(mo =>
    api(`/v2/accounts/${cfg.account}/reports/summary?type=agent&id=${id}&since=${mo.since}&until=${mo.until}`)
      .catch(() => null)
  ));
  if (!_agentMonthCache[id]) _agentMonthCache[id] = {};
  _agentMonthCache[id][key] = months.map((mo, i) => ({ ...mo, data: results[i] }));
  if (loadEl) loadEl.style.display = 'none';
}
```

- [ ] **Step 3: Adicionar renderAgentModalHeader()**

```javascript
function renderAgentModalHeader(id) {
  const el = document.getElementById('am-header');
  if (!el) return;
  const entry = _agentPerfData?.[id];
  if (!entry) return;
  const s   = entry.summary;
  const sc  = calcAgentScore(s);
  const scCls = sc == null ? 'badge-na' : sc >= 75 ? 'score-hi' : sc >= 50 ? 'score-mid' : 'score-lo';
  const ini = (entry.agent.name || '?')[0].toUpperCase();

  const tmaH = s?.avg_resolution_time != null ? s.avg_resolution_time / 3600 : null;
  const tmrM = s?.avg_first_response_time != null ? s.avg_first_response_time / 60 : null;
  const fcr  = s?.conversations_count > 0 ? (s.resolutions_count || 0) / s.conversations_count * 100 : null;

  const dot = (val, meta, lowerIsBetter) => {
    if (val == null) return `<span style="width:9px;height:9px;border-radius:50%;background:var(--bg3);display:inline-block"></span>`;
    const ratio = lowerIsBetter ? val / meta : meta / val;
    const col   = ratio <= 1 ? 'var(--green)' : ratio <= 1.5 ? 'var(--yellow)' : 'var(--red)';
    return `<span style="width:9px;height:9px;border-radius:50%;background:${col};display:inline-block"></span>`;
  };

  const idx  = _agentModalOrder.indexOf(id);
  const prev = _agentModalOrder[idx - 1] != null;
  const next = _agentModalOrder[idx + 1] != null;
  const period = { '1': 'Mês atual', '3': '3 meses', '6': '6 meses', '12': '12 meses' }[_agentTabPeriod];

  el.innerHTML = `
    <div class="am-avatar">${ini}</div>
    <div>
      <div class="am-name">${esc(entry.agent.name)}</div>
      <div style="font-size:11px;color:var(--text2)">${period}</div>
    </div>
    <span class="score-badge ${scCls}" style="font-size:18px;padding:4px 14px">${sc ?? '—'}</span>
    <div class="am-semaphores">
      ${dot(tmaH, _SLA_TMA, true)} <span style="font-size:11px;color:var(--text2)">Atend.</span>
      ${dot(tmrM, _SLA_TMR, true)} <span style="font-size:11px;color:var(--text2)">Resposta</span>
      ${dot(fcr,  _SLA_FCR, false)}<span style="font-size:11px;color:var(--text2)">Resolução</span>
    </div>
    <div class="am-nav">
      <button onclick="navigateAgentModal(-1)" ${prev ? '' : 'disabled'} title="Agente anterior (←)">‹</button>
      <button onclick="navigateAgentModal(1)"  ${next ? '' : 'disabled'} title="Próximo agente (→)">›</button>
    </div>
    <button class="am-close" onclick="closeAgentModal(null)" title="Fechar (ESC)">×</button>`;
}
```

- [ ] **Step 4: Adicionar renderAgentModalHighlights()**

```javascript
function renderAgentModalHighlights(id, months) {
  const el = document.getElementById('am-highlights');
  if (!el) return;
  if (!months) {
    el.innerHTML = '<div class="am-highlight" style="color:var(--text3)">Calculando destaques…</div>';
    return;
  }
  const withData = months.filter(m => m.data && (m.data.conversations_count || 0) > 0);
  if (!withData.length) {
    el.innerHTML = '<div class="am-highlight" style="color:var(--text3)">Sem dados no período</div>';
    return;
  }
  const scored = withData
    .map(m => ({ label: m.label, score: calcAgentScore(m.data) }))
    .filter(m => m.score != null);
  if (!scored.length) { el.innerHTML = ''; return; }

  const best  = scored.reduce((a, b) => b.score > a.score ? b : a);
  const worst = scored.reduce((a, b) => b.score < a.score ? b : a);

  let trend = '';
  const last = withData[withData.length - 1];
  const prev = withData[withData.length - 2];
  if (last && prev) {
    const sc1 = calcAgentScore(last.data), sc2 = calcAgentScore(prev.data);
    if (sc1 != null && sc2 != null) {
      if (sc1 > sc2)      trend = `<div class="am-highlight"><strong>↑ Melhorando</strong>Pontuação subiu de ${sc2} para ${sc1} no último mês</div>`;
      else if (sc1 < sc2) trend = `<div class="am-highlight"><strong>↓ Queda recente</strong>Pontuação caiu de ${sc2} para ${sc1} no último mês</div>`;
      else                trend = `<div class="am-highlight"><strong>→ Estável</strong>Pontuação mantida no último mês</div>`;
    }
  }

  el.innerHTML = `
    <div class="am-highlight"><strong>🟢 Melhor mês: ${best.label}</strong>Pontuação ${best.score}/100</div>
    <div class="am-highlight"><strong>🔴 Pior mês: ${worst.label}</strong>Pontuação ${worst.score}/100</div>
    ${trend}`;
}
```

- [ ] **Step 5: Adicionar renderAgentModalChart()**

```javascript
function renderAgentModalChart(id) {
  destroyChart('am');
  const months = _agentMonthCache[id]?.[_agentTabPeriod];
  const canvas = document.getElementById('am-chart');
  if (!months || !canvas) return;
  const c      = chartColors();
  const labels = months.map(m => m.label);
  const vol    = months.map(m => m.data?.conversations_count || 0);
  const tma    = months.map(m => m.data?.avg_resolution_time != null
    ? +(m.data.avg_resolution_time / 3600).toFixed(2) : null);
  const tmr    = months.map(m => m.data?.avg_first_response_time != null
    ? +(m.data.avg_first_response_time / 60).toFixed(1) : null);

  const annotationPlugin = window['chartjs-plugin-annotation'];
  _charts['am'] = new Chart(canvas, {
    type: 'bar',
    data: { labels, datasets: [
      { type: 'bar',  label: 'Volume', data: vol,
        backgroundColor: 'rgba(59,130,246,0.2)', borderColor: 'rgba(59,130,246,0.5)',
        borderWidth: 1, borderRadius: 4, yAxisID: 'y2' },
      { type: 'line', label: `Atendimento (h) · meta ${_SLA_TMA}h`, data: tma,
        borderColor: '#8b5cf6', backgroundColor: 'transparent',
        tension: 0.3, yAxisID: 'y1', pointRadius: 4, spanGaps: true, borderWidth: 2 },
      { type: 'line', label: `1ª Resposta (min) · meta ${_SLA_TMR}min`, data: tmr,
        borderColor: '#14b8a6', backgroundColor: 'transparent',
        tension: 0.3, yAxisID: 'y1', pointRadius: 4, spanGaps: true, borderWidth: 2 }
    ]},
    options: {
      responsive: true, maintainAspectRatio: false, animation: false,
      plugins: {
        legend: { display: true, position: 'top',
          labels: { color: c.labels, font: { size: 11 }, boxWidth: 12 } },
        tooltip: { backgroundColor: c.tooltipBg, titleColor: c.labels,
          bodyColor: c.labels, borderColor: c.grid, borderWidth: 1 },
        annotation: annotationPlugin ? { annotations: {
          slaTma: { type: 'line', yMin: _SLA_TMA, yMax: _SLA_TMA, yScaleID: 'y1',
            borderColor: 'rgba(139,92,246,0.5)', borderWidth: 1.5, borderDash: [6,4] },
          slaTmr: { type: 'line', yMin: _SLA_TMR, yMax: _SLA_TMR, yScaleID: 'y1',
            borderColor: 'rgba(20,184,166,0.5)', borderWidth: 1.5, borderDash: [6,4] }
        }} : {}
      },
      scales: {
        x:  { grid: { color: c.grid }, ticks: { color: c.labels, font: { size: 11 } } },
        y1: { position: 'left',  grid: { color: c.grid },
          ticks: { color: c.labels, font: { size: 11 } },
          title: { display: true, text: 'h / min', color: c.labels, font: { size: 10 } } },
        y2: { position: 'right', grid: { display: false },
          ticks: { color: c.labels, font: { size: 11 } },
          title: { display: true, text: 'Volume', color: c.labels, font: { size: 10 } } }
      }
    }
  });
}
```

- [ ] **Step 6: Adicionar renderAgentModalMonthTable()**

```javascript
function renderAgentModalMonthTable(id) {
  const el = document.getElementById('am-month-table-wrap');
  if (!el) return;
  const months = _agentMonthCache[id]?.[_agentTabPeriod];
  if (!months) { el.innerHTML = ''; return; }

  const fmtCell = (value, label, meta, lowerIsBetter) => {
    if (value == null || label == null) return '<span class="agent-badge badge-na">—</span>';
    const ratio  = lowerIsBetter ? value / meta : meta / value;
    const cls    = ratio <= 1 ? 'badge-green' : ratio <= 1.5 ? 'badge-yellow' : 'badge-red';
    const pct    = Math.round(Math.abs(ratio - 1) * 100);
    const dir    = lowerIsBetter
      ? (ratio <= 1 ? `${pct}% abaixo da meta ✓` : `${pct}% acima da meta ✗`)
      : (ratio <= 1 ? `${pct}% abaixo da meta ✗` : `${pct}% acima da meta ✓`);
    return `<span class="agent-badge ${cls}" title="${dir}">${label}</span>`;
  };

  el.innerHTML = `
    <table class="am-month-table">
      <thead><tr>
        <th>Mês</th>
        <th>Volume</th>
        <th title="Tempo Médio de Atendimento · meta: ${_SLA_TMA}h">Atendimento</th>
        <th title="Tempo Médio de 1ª Resposta · meta: ${_SLA_TMR}min">1ª Resposta</th>
        <th title="Taxa de Resolução · meta: ${_SLA_FCR}%">Resolução</th>
        <th title="Pontuação geral 0–100">Pontuação</th>
      </tr></thead>
      <tbody>
        ${months.map(m => {
          const d  = m.data;
          const nd = !d || (d.conversations_count || 0) === 0;
          const tmaH = d?.avg_resolution_time != null ? d.avg_resolution_time / 3600 : null;
          const tmrM = d?.avg_first_response_time != null ? d.avg_first_response_time / 60 : null;
          const fcr  = !nd && d.conversations_count > 0
            ? (d.resolutions_count || 0) / d.conversations_count * 100 : null;
          const sc   = nd ? null : calcAgentScore(d);
          const scCls = sc == null ? 'badge-na' : sc >= 75 ? 'score-hi' : sc >= 50 ? 'score-mid' : 'score-lo';
          return `<tr>
            <td style="font-weight:600;white-space:nowrap">${m.label}</td>
            <td>${nd ? '—' : d.conversations_count.toLocaleString('pt-BR')}</td>
            <td>${nd ? '<span class="agent-badge badge-na">—</span>'
              : fmtCell(tmaH, fmtSec(d.avg_resolution_time), _SLA_TMA, true)}</td>
            <td>${nd ? '<span class="agent-badge badge-na">—</span>'
              : fmtCell(tmrM, fmtSec(d.avg_first_response_time), _SLA_TMR, true)}</td>
            <td>${fcr == null ? '<span class="agent-badge badge-na">—</span>'
              : fmtCell(100 - fcr, Math.round(fcr) + '%', 100 - _SLA_FCR, true)}</td>
            <td>${sc == null ? '<span class="agent-badge badge-na">—</span>'
              : `<span class="score-badge ${scCls}">${sc}</span>`}</td>
          </tr>`;
        }).join('')}
      </tbody>
    </table>`;
}
```

- [ ] **Step 7: Verificar fluxo completo do modal**

1. Abrir http://localhost:8765, clicar em "Agentes"
2. Clicar em qualquer linha ou "Ver perfil →" → modal deve abrir
3. Modal mostra: avatar com inicial, pontuação, 3 semáforos, gráfico combinado, destaques, tabela mensal
4. Clicar setas ‹ › navega entre agentes sem fechar o modal
5. ESC fecha o modal
6. Clicar no backdrop (área escurecida fora do modal) fecha
7. DevTools Network: ao abrir o modal, aparecem as chamadas mensais em paralelo; na segunda abertura do mesmo agente no mesmo período, **nenhuma chamada nova** (cache)

- [ ] **Step 8: Commit**

```bash
git add index.html
git commit -m "feat(agentes): modal Perfil do Agente — header, destaques, gráfico, tabela mensal"
```

---

## Task 7: Limpeza Final — Remover Funções v1, Atualizar setAgentTabPeriod()

**Files:**
- Modify: `index.html` (JS — remoções e ajustes finais)

**Interfaces:**
- Remove: `renderAgentLive()`, `toggleAgentExpand()`, `renderAgentExpandChart()`
- Updates: `setAgentTabPeriod()` — limpar `_agentMonthCache` ao mudar período; `renderAgentTab()` — chamar só renderAgentPerf + renderAgentCsat

- [ ] **Step 1: Remover renderAgentLive()**

Localizar `function renderAgentLive()` e apagar a função inteira (da linha `function renderAgentLive() {` até o `}` de fechamento, ~75 linhas).

- [ ] **Step 2: Remover toggleAgentExpand() e renderAgentExpandChart()**

Localizar `function toggleAgentExpand(id)` e apagar essa função inteira.
Localizar `function renderAgentExpandChart(id)` e apagar essa função inteira.

- [ ] **Step 3: Atualizar setAgentTabPeriod()**

Localizar `function setAgentTabPeriod(p)` e substituir por:

```javascript
function setAgentTabPeriod(p) {
  _agentTabPeriod  = p;
  _agentPerfData   = null;
  _agentMonthCache = {};
  if (_agentModalId !== null) {
    closeAgentModal(null);
  }
  const labels = { '1': 'Mês atual', '3': '3 meses', '6': '6 meses', '12': '12 meses' };
  document.querySelectorAll('.agent-pill').forEach(btn => {
    btn.classList.toggle('active', btn.textContent.trim() === labels[p]);
  });
  fetchAgentTab();
}
```

- [ ] **Step 4: Atualizar renderAgentTab()**

Localizar `function renderAgentTab()` e substituir por:

```javascript
function renderAgentTab() {
  renderAgentPerf();
  renderAgentCsat();
}
```

- [ ] **Step 5: Verificar que não há referências órfãs**

No console do browser, verificar que as seguintes funções NÃO existem mais:
```javascript
typeof renderAgentLive     // "undefined"
typeof toggleAgentExpand   // "undefined"
typeof renderAgentExpandChart // "undefined"
```

E que `_agentOverview` e `_agentExpanded` NÃO existem mais como variáveis globais:
```javascript
typeof _agentOverview  // "undefined"
typeof _agentExpanded  // "undefined"
```

- [ ] **Step 6: Commit final**

```bash
git add index.html
git commit -m "refactor(agentes): remover funções v1 — renderAgentLive, toggleAgentExpand, renderAgentExpandChart"
```

---

## Self-Review

**Spec coverage:**
- ✅ Remove seção Status ao vivo (HTML + função + API call)
- ✅ Tabela com badges coloridos (badge-green/yellow/red)
- ✅ Coluna Pontuação 0–100 com calcAgentScore()
- ✅ Ordenação padrão: Pontuação desc
- ✅ Linguagem sem jargão: "Atendimento", "1ª Resposta", "Resolução"
- ✅ Tooltip em cada coluna com nome completo + meta
- ✅ Botão "Ver perfil →" por linha
- ✅ Fetch paralelo: summaries + CSAT em Promise.all simultâneo
- ✅ Sem chamada para /overview
- ✅ Dados mensais lazy em fetchAgentMonths()
- ✅ Cache _agentMonthCache[id][period]
- ✅ Cache limpo ao mudar período
- ✅ Modal com avatar + pontuação + semáforos + navegação ‹ ›
- ✅ ESC e clique no backdrop fecham o modal
- ✅ Destaques: melhor mês, pior mês, tendência
- ✅ Gráfico combinado: Volume (barras) + TMA/TMR (linhas) com linhas SLA
- ✅ Tabela mensal com badges e % vs meta
- ✅ Agentes sem dados no período: pontuação "—", no final da tabela
- ✅ Erro de auth: mensagem 🔒

**Gaps:** Nenhum.
